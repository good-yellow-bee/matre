{% extends 'admin/base.html.twig' %}
{% block title %}Categories{% endblock %}
{% block body %}
<div class="d-flex justify-content-between mb-4">
    <h1>Categories</h1>
    <a href="{{ path('admin_category_new') }}" class="btn btn-primary">Create New</a>
</div>

<div 
    data-vue-island="category-grid"
    data-api-url="{{ path('api_categories_list') }}"
    data-csrf-token="{{ csrf_token('toggle') }}"
></div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ vite_entry_script_tags('category-grid-app') }}
{% endblock %}
